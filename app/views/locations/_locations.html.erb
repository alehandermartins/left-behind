<% locations.each_slice(2) do |pair| %>
  <%  pair.each do |location| %>
    <%= link_to trans("locations.#{location.index}.name"), location_path(location), data: {"turbo-stream": true }, class: "bg-blue-500 hover:bg-blue-700 text-white font-bold py-3 px-6 rounded-lg hover:cursor-pointer transition duration-300 ease-in-out hover:bg-blue-700 transform hover:-translate-y-1 hover:shadow-lg" %>
  <% end %>
<% end %>
