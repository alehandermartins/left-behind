<!DOCTYPE html>
<html>
  <head>
    <title>LeftBehind</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="flex flex-col min-h-screen">

      <nav class="bg-gray-800 text-white h-16 flex items-center text-center justify-evenly">
        <div class="text-white font-bold text-xl px-4">
          Oxygen <span class="font-bold" id="oxygen_quantity"><%= oxygen %></span>
        </div>
        <%= render partial: "shared/go_back", locals: {hidden: true} %>
      </nav>

      <%= yield %>

      <footer class="bg-gray-800 text-white h-48 flex items-center justify-center text-center">
        <p id="logs">Awaiting data...</p>
      </footer>
    </div>
  </body>
</html>
