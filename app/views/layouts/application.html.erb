<!DOCTYPE html>
<html>
  <head>
    <title>LeftBehind</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
    <div class="flex flex-col min-h-screen">
      <nav class="bg-gray-800 text-white h-16 flex items-center text-center justify-evenly w-full">
        <div class="w-1/3">
          <%= yield :back_home %>
        </div>
        <div class="w-1/3 text-white font-bold text-xl">
          Left Behind
        </div>
        <div class="w-1/3 text-white text-xl rounded-md">
        </div>
      </nav>

      <%= render "shared/flash" %>

      <div class="flex-grow bg-blue-200 flex flex-col justify-evenly items-center">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
